void kesu() {
  if ((mouseX > 30) && (mouseX < 80) && (mouseY > 130) && (mouseY < 150)) {
    saveText[1] = "・";
    saveStrings("save", saveText);
    Text();
  } else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 180) && (mouseY < 200)) {
    saveText[2] = "・";
    saveStrings("save.txt", saveText);
    Text();
  } else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 230) && (mouseY < 250)) {
    saveText[3] = "・";
    saveStrings("save.txt", saveText);
    Text();
  } else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 280) && (mouseY < 300)) {
    saveText[4] = "・";
    saveStrings("save.txt", saveText);
    Text();
  } else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 330) && (mouseY < 350)) {
    saveText[5] = "・";
    saveStrings("save.txt", saveText);
    Text();
  } else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 380) && (mouseY < 400)) {
    saveText[6] = "・";
    saveStrings("save.txt", saveText);
    Text();
  }else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 430) && (mouseY < 450)) {
    saveText[7] = "・";
    saveStrings("save.txt", saveText);
    Text();
  }else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 480) && (mouseY < 500)) {
    saveText[8] = "・";
    saveStrings("save.txt", saveText);
    Text();
  }else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 530) && (mouseY < 550)) {
    saveText[9] = "・";
    saveStrings("save.txt", saveText);
    Text();
  }else if ((mouseX > 30) && (mouseX < 80) && (mouseY > 580) && (mouseY < 600)) {
    saveText[10] = "・";
    saveStrings("save.txt", saveText);
    Text();
  }
}
